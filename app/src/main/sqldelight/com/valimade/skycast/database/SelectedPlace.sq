CREATE TABLE selected_place(
  id INTEGER PRIMARY KEY NOT NULL,
  name TEXT NOT NULL,
  latitude REAL NOT NULL,
  longitude REAL NOT NULL
);

selectAll:
SELECT * FROM selected_place;

insert:
INSERT INTO selected_place(id, name, latitude, longitude)
VALUES (?, ?, ?, ?);

deleteById:
DELETE FROM selected_place
WHERE id = ?; 